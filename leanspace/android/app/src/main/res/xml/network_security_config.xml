<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
        Lean Space network security policy.

        We pin to the exact HTTPS endpoints we use so we don't allow
        arbitrary cleartext traffic. Play Billing's verify endpoint is
        Google-hosted.
    -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">workers.dev</domain>
        <domain includeSubdomains="true">leanspace.app</domain>
        <domain includeSubdomains="true">googleapis.com</domain>
        <domain includeSubdomains="true">google.com</domain>
    </domain-config>
</network-security-config>
