# Copy this file to `.env` in the same directory (never commit `.env`).
# Get values from Supabase Dashboard → Project Settings → API.

SUPABASE_URL=https://your-project-ref.supabase.co
# Preferred (Dashboard → Project Settings → API → Publishable key)
SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxx
# Legacy JWT anon key also works if publishable key is not set
SUPABASE_ANON_KEY=your-anon-key-here

# Phase 2+
# ADMOB_BANNER_UNIT_ID_ANDROID=ca-app-pub-xxx/yyy

# Phase 4+
# SENTRY_DSN=https://xxx@yyy.ingest.sentry.io/zzz
