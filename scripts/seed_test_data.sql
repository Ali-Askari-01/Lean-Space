-- LeanSpace test data seed (no secrets).
-- Run against your Supabase project after creating test users via Auth.
-- See execution/11-DEV-SETUP.md §4.

-- Example: truncate and re-seed test user todos (uncomment when tables exist)
-- DELETE FROM public.todos WHERE user_id IN (
--   SELECT id FROM public.users WHERE email LIKE 'test-%@leanspace.dev'
-- );
